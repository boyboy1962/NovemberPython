Dictionary(사전형)
 - 데이터의 집합
 - key와 value값의 형태로 1:1 쌍으로 저장하여 사용한다.
 - key값은 충복 불가.. 변경 불가
 - value값은 중복 허용.. 변경 가능

선언
 - {}을 통하여 선언하면 된다.
 - key와 value를 구별하기 위해서 중간에 :을 적어준다.
	변수명 = {key : value, key : value, key : value}

호출
 - key를 통해서 호출하여야한다.
 - 형식
	변수명[key]
